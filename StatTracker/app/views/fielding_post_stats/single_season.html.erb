<% title "Fielding Post Stats" %>

<table>
  <tr>
    <th>Player</th>
    <th>Team</th>
	<th>Year</th>
    <th>Round</th>
	<th><%= params[:stat].titleize %></th>
  </tr>
  <% for fielding_post_stat in @fielding_post_stats %>
    <tr>
      <td><%= fielding_post_stat.player.last_name %></td>
      <td><%= fielding_post_stat.team.name %></td>
	  <td><%= fielding_post_stat.team.year %></td>
      <td><%= fielding_post_stat.round %></td>
	  <td><%= fielding_post_stat.send(params[:stat])%></td>
    </tr>
  <% end %>
</table>
